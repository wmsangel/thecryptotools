import type { ToolConfig } from "../types";

/**
 * Optional home for tools defined inline (e.g. ones generated in chat and
 * pasted here) instead of as their own file in `configs/`. Both `builtinTools`
 * and `generatedTools` are merged in `registry.ts`, so anything you add here
 * becomes a full static page.
 *
 * Entries here should use `expression` (a safe formula string) so they stay
 * copy-paste friendly, but `compute` functions work too.
 */
export const generatedTools: ToolConfig[] = [
  // Example (kept commented so the array is empty by default):
  // {
  //   slug: "ai-example-tool",
  //   title: "AI Example Tool",
  //   description: "Generated by /api/tools/generate.",
  //   category: "ai",
  //   seo: { keywords: ["example"], description: "An AI generated tool." },
  //   inputs: [{ name: "x", type: "number", default: 1 }],
  //   expression: "x * 2",
  //   faq: [],
  //   source: "ai",
  // },
];
